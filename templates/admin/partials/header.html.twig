<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('homepage') }}">
        <img src="/img-template/logo.png" alt="">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse pt-2" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('admin_dashboard') }}">
                    <i class="fa fa-line-chart"></i> Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_ads_index') }}" class="nav-link">
                    <i class="fa fa-building-o"></i> Annonces
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_booking_index') }}" class="nav-link">
                    <i class="fa fa-folder"></i> Réservations
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_comments_index') }}" class="nav-link">
                    <i class="fa fa-comments"></i> Commentaires
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_user_index') }}" class="nav-link">
                    <i class="fa fa-users"></i> Utilisateurs
                </a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto fa-ul">
            <li class="nav-item margR">
                <a href="{{ path('homepage') }}" target="_blank" class="nav-link">
                    <i class="fa fa-eye" aria-hidden="true"></i> Voir le site
                </a>
            </li>
                {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {% if app.user.profilPicture == null %}
                            <div class="home-avatar-guest"></div>
                        {% else %}
                            <img src="{{ vich_uploader_asset(app.user, 'imageFile') | imagine_filter('home_avatar') }}" class="rounded-circle" alt="Avatar de {{ app.user.getFullName }}">
                        {% endif %}
                        
                        Bonjour {{ app.user.firstName | capitalize }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('logout') }}" class="dropdown-item"><i class="fa fa-sign-out" aria-hidden="true"></i> Déconnexion</a>
                    </div>
                </li>
                {% endif %}
        </ul>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }} text-center" style="margin: 0 !important;">
            {% for message in messages %}
                <p style="margin: 0 !important;">{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %} 